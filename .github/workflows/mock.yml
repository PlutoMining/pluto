name: Mock CI

on:
  push:
    branches:
      - main
      - develop
      - "release/**"
  pull_request:
    branches:
      - "**"

permissions:
  contents: read
  actions: write

jobs:
  test-mock:
    name: Test Mock
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mock/package-lock.json

      - name: Install shared common packages
        run: |
          for pkg in common/interfaces common/logger; do
            if [ -f "$pkg/package-lock.json" ]; then
              echo "Installing deps for $pkg"
              (cd "$pkg" && npm ci)
              echo "Building $pkg"
              (cd "$pkg" && npm run build)
            fi
          done

      - name: Install mock dependencies
        working-directory: mock
        run: npm ci

      - name: Build TypeScript
        working-directory: mock
        run: npm run build

      - name: Run tests with coverage
        working-directory: mock
        run: npm run test:coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: mock-coverage
          path: mock/coverage

      - name: Coverage summary (Octocov)
        uses: k1LoW/octocov-action@v1
        with:
          config: ../.octocov/mock.yml
          work-dir: mock
