/**
 * Utility types that were part of the old device-info module.
 * Only DropdownOption is retained; legacy Device* types are removed.
 */

export interface DropdownOption {
  label: string;
  value: number;
}
