fastapi==0.104.1
uvicorn==0.24.0
# Use PlutoMining fork of pyasic instead of upstream PyPI package.
# This branch includes the partial-data fix (get_data() returns None for
# failed sub-calls instead of raising APIError).
# Pin to a specific commit with e.g. "refs/tags/v0.78.1-pluto1.zip"
pyasic @ https://github.com/PlutoMining/pyasic/archive/refs/heads/feature/support-partial-data.zip
pydantic>=2.12.5,<3.0.0
websockets>=12.0,<13.0
