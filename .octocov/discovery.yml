coverage:
  paths:
    - coverage/lcov.info
  acceptable: diff >= -0.5%

summary:
  if: true
  hideFooterLink: true

diff:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}/octocov-discovery

report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}/octocov-discovery
